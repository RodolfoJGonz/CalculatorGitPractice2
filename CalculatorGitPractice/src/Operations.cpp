#include "Operations.h"


int Operations::addition(int x, int y) {
	return (x + y);
}

int Operations::subtraction(int x, int y) {
	return (x - y);
}

int Operations::multiplication(int x, int y) {
	return (x * y);
}

int Operations::division(int x, int y) {
	return (x / y);
}